# Build defaults: Override by options on the make command
BOARD=vgaboard_sd_w
CYW43=GPIO
LFS=Y
FAT=Y
SOUND=PWM
SERIAL_DEV=0
PRINTER=N
MIN_STACK=Y
SUFFIX=_pkg

# Required for GUI build
STDIO=PICO

# Select files loaded into LFS
LFS_FILES=../../bin/pico/examples/*
LFS_EXTRA=../../bin/pico/examples

include ../../src/pico/Makefile
